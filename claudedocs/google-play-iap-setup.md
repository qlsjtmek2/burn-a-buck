# Google Play Console 인앱 상품 등록 가이드

## 📋 개요

"천원 쓰레기통" 앱의 Google Play 인앱 결제 상품(₩1,000 기부)을 등록하는 단계별 가이드입니다.

---

## 🔑 사전 준비사항

### 1. Google Play Console 계정
- [ ] Google Play Console 개발자 계정 생성 완료
- [ ] 일회성 등록 수수료 $25 지불 완료
- [ ] 앱 등록 완료 (패키지명: `com.yourcompany.burnabuck` 또는 실제 패키지명)

### 2. 결제 프로필 설정
- [ ] Google Play Console > 설정 > 판매자 계정 생성
- [ ] 사업자 정보 입력 (개인 또는 사업자)
- [ ] 은행 계좌 정보 등록 (수익 정산용)
- [ ] 세금 정보 제출

---

## 📦 인앱 상품 등록 단계

### 1단계: Google Play Console 접속

1. [Google Play Console](https://play.google.com/console) 접속
2. "천원 쓰레기통" 앱 선택
3. 좌측 메뉴에서 **수익 창출 > 인앱 상품** 클릭

### 2단계: 새 상품 만들기

1. **"관리형 제품 만들기"** 버튼 클릭
2. 제품 세부정보 입력:

   | 필드 | 값 |
   |------|-----|
   | **제품 ID** | `donate_1000won` |
   | **이름** | `천원 기부` |
   | **설명** | `천원을 기부하고 감사 메시지를 받습니다` |
   | **상태** | `활성` (테스트 완료 후 활성화) |
   | **기본 가격** | `₩1,000` (KRW) |

3. **"만들기"** 클릭

### 3단계: 가격 설정

1. 생성된 상품 클릭
2. **"가격 설정"** 탭 선택
3. 기본 가격 입력:
   - **통화**: 한국 원화 (KRW)
   - **가격**: ₩1,000

4. (선택사항) 다른 국가 가격 추가:
   - **미국 (USD)**: $1.00
   - **일본 (JPY)**: ¥100
   - 또는 **"현지 가격 자동 변환"** 활성화

5. **"적용"** 클릭

### 4단계: 상품 활성화

1. 상품 상태를 **"활성"**으로 변경
2. **"저장"** 클릭
3. 변경사항 검토 후 **"게시"** 클릭

---

## 🧪 테스트 설정

### 라이선스 테스터 추가

프로덕션 환경에 배포하기 전에 결제를 테스트해야 합니다.

1. **설정 > 라이선스 테스트** 메뉴 이동
2. **"라이선스 테스터 추가"** 클릭
3. Gmail 계정 입력 (실제 결제되지 않음):
   ```
   your.test.email@gmail.com
   ```
4. **"저장"** 클릭

### 테스트 방법

1. **Internal Testing Track** 배포:
   - EAS Build로 AAB 파일 생성
   - Google Play Console > 테스트 > 내부 테스트 트랙에 업로드
   - 라이선스 테스터 계정을 내부 테스터로 추가

2. **테스트 디바이스에서 확인**:
   - 라이선스 테스터 계정으로 Google Play 로그인
   - 내부 테스트 앱 다운로드
   - 결제 플로우 테스트 (실제 결제되지 않음)
   - 영수증 검증 및 Supabase 저장 확인

---

## 🔐 서비스 계정 키 설정 (영수증 검증용)

Google Play Developer API를 사용하여 서버에서 영수증을 검증하려면 서비스 계정이 필요합니다.

### 1단계: 서비스 계정 생성

1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 프로젝트 선택 또는 새 프로젝트 생성
3. **APIs & Services > 사용자 인증 정보** 메뉴 이동
4. **"사용자 인증 정보 만들기" > 서비스 계정** 선택
5. 서비스 계정 정보 입력:
   - 이름: `burn-a-buck-receipt-validator`
   - 역할: 없음 (나중에 Google Play Console에서 설정)
6. **"완료"** 클릭

### 2단계: JSON 키 생성

1. 생성된 서비스 계정 클릭
2. **"키 추가" > 새 키 만들기** 선택
3. **JSON** 형식 선택
4. 생성된 JSON 파일 다운로드 (안전한 곳에 보관)

### 3단계: Google Play Console에서 권한 부여

1. Google Play Console > 설정 > API 액세스 이동
2. **"서비스 계정 연결"** 클릭
3. 생성한 서비스 계정 선택
4. 권한 설정:
   - **"재무 데이터 보기"** 체크
   - **"주문 및 정기 결제 관리"** 체크
5. **"권한 부여"** 클릭

### 4단계: Supabase Edge Function에 업로드

JSON 키를 Supabase Edge Function의 환경 변수로 설정:

```bash
# Supabase CLI 사용
supabase secrets set GOOGLE_SERVICE_ACCOUNT_KEY="$(cat path/to/service-account-key.json)"
```

또는 Supabase Dashboard에서:
1. Settings > Edge Functions
2. Environment Variables 섹션
3. `GOOGLE_SERVICE_ACCOUNT_KEY` 추가 (JSON 전체 내용 붙여넣기)

---

## 📝 제품 ID 확인

앱 코드에서 사용할 제품 ID:

```typescript
// src/constants/payment.ts
export const PRODUCT_IDS = {
  ANDROID: 'donate_1000won',
  IOS: 'com.yourcompany.burnabuck.donate_1000won', // iOS는 나중에 추가
};

export const DONATION_AMOUNT = 1000; // KRW
```

---

## ✅ 체크리스트

배포 전 확인사항:

- [x] Google Play Console 개발자 계정 생성
- [x] 판매자 계정 및 결제 정보 등록
- [ ] 인앱 상품 `donate_1000won` 생성 및 활성화
- [ ] 가격 ₩1,000 설정
- [ ] 라이선스 테스터 계정 추가
- [ ] Internal Testing Track에서 결제 테스트 완료
- [ ] 서비스 계정 생성 및 JSON 키 다운로드
- [ ] Google Play Console에서 서비스 계정 권한 부여
- [ ] Supabase Edge Function에 서비스 계정 키 환경 변수 설정

---

## 🚨 주의사항

### 1. 테스트 vs 프로덕션
- **라이선스 테스터**: 실제 결제되지 않음, 테스트 전용
- **프로덕션**: 실제 사용자는 진짜 결제 진행

### 2. 환불 정책
- Google Play는 48시간 이내 환불 가능
- 환불 시 앱에서 적절히 처리 필요 (Supabase에서 기록 삭제 또는 비활성화)

### 3. 세금 및 수수료
- Google Play 수수료: 15% (연간 $1M 이하), 30% (그 이상)
- 부가가치세(VAT) 자동 계산 및 공제
- 정산: 월 1회 은행 계좌로 입금

### 4. 보안
- 서비스 계정 JSON 키는 **절대 Git에 커밋하지 말 것**
- `.gitignore`에 `*.json` 또는 `service-account-key.json` 추가
- Supabase Edge Function 환경 변수로만 관리

---

## 📚 참고 자료

- [Google Play Billing 문서](https://developer.android.com/google/play/billing)
- [react-native-iap 공식 문서](https://react-native-iap.dooboolab.com/)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Google Play Developer API](https://developers.google.com/android-publisher)

---

**작성일**: 2025-11-03
**버전**: 1.0.0
